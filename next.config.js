/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // This completely disables the Next.js development indicator
  devIndicators: false
}

module.exports = nextConfig